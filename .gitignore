# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# opennext / cloudflare
.open-next/
.wrangler/

# env
.env
.env*.local
.dev.vars

# testing
coverage/
playwright-report/
test-results/

# TypeScript incremental build cache
*.tsbuildinfo

# misc
.DS_Store
*.pem
npm-debug.log*

# Next.js が `next dev` 起動時にリポジトリ直下へ自動生成するエージェント規約ファイル。
# 生成物であり、この リポジトリの規約（docs/ 配下）とは別物なので追跡しない。
/AGENTS.md
/CLAUDE.md

# task harness locks (mkdir-based exclusive locks; never committed)
.locks/

# editor
.vscode/*
!.vscode/extensions.json
.idea/
